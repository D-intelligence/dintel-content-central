# Multi-Agent Development Framework

**Project:** dintel-content-central (Strapi CMS)
**Version:** 1.0
**Last Updated:** 2026-01-31

---

## 1. Agent Hierarchy

```
                    Human (Final Authority)
                           │
                           ▼
                    ┌─────────────┐
                    │ Claude Code │ ← Lead Agent
                    │ Orchestrator│
                    └──────┬──────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
    ┌─────────┐      ┌─────────┐      ┌─────────┐
    │ Gemini  │      │  Codex  │      │  Human  │
    │Research │      │  Speed  │      │ Approve │
    └─────────┘      └─────────┘      └─────────┘
```

| Agent | Role | Scope |
|-------|------|-------|
| **Claude** | Lead / Orchestrator | Architecture, API design, content types, integration |
| **Gemini** | Sub-agent | Documentation, Strapi guides, API research |
| **Codex** | Sub-agent | Tests, components, utilities, type definitions |
| **Human** | Authority | Business decisions, content model approval |

---

## 2. File Ownership (Strapi Project)

### 2.1 Ownership Matrix

| Pattern | Owner | Description |
|---------|-------|-------------|
| `src/api/` | Claude | Content-type APIs, controllers, services |
| `src/admin/` | Claude | Admin panel customizations |
| `src/extensions/` | Claude | Plugin extensions |
| `src/bootstrap.js`, `src/index.js` | Claude | Application lifecycle |
| `config/` | Claude | Server, database, middleware config |
| `src/components/` | Codex | Reusable content components |
| `types/` | Codex | TypeScript type definitions |
| `scripts/` | Codex | Utility scripts, seeds |
| `tests/` | Codex | All test files |
| `docs/` | Gemini | User and API documentation |
| `README.md` | Gemini | Project documentation |

### 2.2 Shared Files (Require Claude Approval)

| Files | Rule |
|-------|------|
| `package.json`, `yarn.lock` | Claude approves dependency changes |
| `.github/workflows/` | Claude approves CI/CD changes |
| `config/database.js` | Claude approves database config |
| `config/server.js` | Claude approves server config |

### 2.3 Unrestricted Files

Any agent can modify: `.agent-state/*`, `.gitignore`, `public/`

---

## 3. State Management

```
.agent-state/
├── tasks.yaml    # Task registry
└── locks.yaml    # File locks
```

### 3.1 Task Entry

```yaml
tasks:
  - id: "TASK-001"
    agent: "codex"
    status: "in_progress"
    description: "Create content type components"
    files: ["src/components/"]
    claimed_at: "2026-01-31T10:00:00Z"
    expires_at: "2026-01-31T22:00:00Z"
```

### 3.2 Lock Types & Expiration

| Type | Duration | Purpose |
|------|----------|---------|
| `exclusive` | 12 hours | Only holder can modify |
| `shared` | 12 hours | Read many, write one |
| `override` | 4 hours | Emergency (Claude only) |

---

## 4. Workflow

### 4.1 Task Claiming

```bash
# 1. Set identity
export AGENT_AUTHOR=codex

# 2. Check state
cat .agent-state/tasks.yaml

# 3. Claim → Work → Complete
git commit -m "[Codex] chore: claim TASK-001"
git commit -m "[Codex] feat(components): add hero section"
git commit -m "[Codex] chore: complete TASK-001"
```

### 4.2 Conflict Resolution

```
1. Ownership Matrix  → Owner has priority
2. Active Locks      → Lock holder has priority
3. Task Assignment   → Assigned agent has priority
4. Escalate to Claude → Final decision
```

---

## 5. Commit Standards

### Format

```
[Agent] type(scope): description

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
```

### Types

| Type | Purpose |
|------|---------|
| `feat` | New feature / content type |
| `fix` | Bug fix |
| `docs` | Documentation |
| `test` | Tests |
| `refactor` | Code refactoring |
| `chore` | Maintenance |

### Agent Tags

`[Claude]`, `[Gemini]`, `[Codex]`, `[Human]`

---

## 6. Quality Gates

### Pre-merge Checklist

- [ ] `npm run build` passes
- [ ] No hardcoded secrets
- [ ] Content types validated
- [ ] Documentation updated
- [ ] Agent attribution in commits

---

## 7. Quick Reference

### Environment Setup

```bash
export AGENT_AUTHOR=claude   # claude | gemini | codex | human
npm install
npm run develop
```

### Strapi Commands

```bash
npm run develop    # Dev server
npm run build      # Build admin
npm run start      # Production
npm run strapi     # Strapi CLI
```

### Ownership Lookup

```
src/api/             → Claude
src/admin/           → Claude
src/extensions/      → Claude
config/              → Claude
src/components/      → Codex
types/               → Codex
scripts/             → Codex
tests/               → Codex
docs/                → Gemini
README.md            → Gemini
package.json         → Shared (Claude approves)
```

---

*Generated by Multi-Agent Guide Skill v2.0*
