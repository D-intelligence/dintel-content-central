# dintel-content-central - Strapi CMS
# Auto-loads when entering this directory

# Load infrastructure secrets from 1Password (Strapi, database)
source ~/.config/secrets/load-from-1password.sh infra 2>/dev/null

# Set NODE_ENV based on current context
export NODE_ENV="${NODE_ENV:-development}"
